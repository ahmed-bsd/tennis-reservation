spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tennis_db
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:ahmedbsd1@gmail.com}
    password: ${MAIL_PASSWORD:mloc fbjp cyal xgck}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

server:
  port: 8081
  servlet:
    context-path: /api

app:
  jwt:
    secret: tennis-app-super-secret-key-very-long-2026-secure
    expiration-ms: 86400000  # 24h

  ollama:
    url: http://localhost:11434/
    model: llama3

  booking:
    max-days-ahead: 2
    opening-hour: 8
    closing-hour: 22
    lighting-start: "17:00"
    lighting-end: "19:30"
    price-morning: 10
    price-evening: 20


springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

weather:
  api:
    key: 56b844d25860f7417fce6d4207cad109
    url: https://api.openweathermap.org/data/2.5/weather
